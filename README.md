# New2data-transformer-sql-project
SQL project for a Corporate Data Analysis System (MySQL) covering Customers, Orders, and Employees. Implements INNER/LEFT/RIGHT/FULL OUTER JOIN (simulated via UNION), subqueries, date/string functions, window functions (running totals, RANK), and CASE expressions.
